# AGENTS.md — Protocolo Operativo del Harness

Guía operativa para agentes que editan este repositorio ({{ project_name }}). Generado
desde [webstack-agent-harness](https://github.com/MrJuancho/webstack-agent-harness) —
las secciones de aquí describen la operación del guantelete en sí (gates, capas de
enforcement, roles); son transversales a cualquier proyecto generado desde este
template y se actualizan vía `copier update`, no reescribiéndolas por proyecto. Lo
específico de {{ project_name }} (dominio, capas de arquitectura reales si difieren del
esqueleto de ejemplo, deuda conocida) va en `docs/adr/` y `docs/progress.md`, no aquí.

Este repositorio opera bajo una arquitectura de **falla cerrada (fail-closed)**. Ninguna tarea se considera completa si no existe verificación reproducible de código (exit 0).

Antes de explorar el repo para reconstruir en qué quedó el trabajo, lee [`docs/progress.md`](./docs/progress.md) — es el handoff entre sesiones (en qué quedó la anterior, qué sigue, qué está bloqueado). Se sobrescribe en cada sesión, no se acumula; al terminar la tuya, actualízalo con el estado real, no lo dejes con el de la sesión pasada.

Para decisiones ya tomadas y su porqué (no solo el qué), ve [`docs/adr/`](./docs/adr/) antes de reabrir un debate ya resuelto o de revertir algo sin entender por qué se hizo así.

---

## 1. Jerarquía de Verificación (`justfile`)

Todo agente debe ejecutar el nivel adecuado según el ciclo de trabajo:

| Nivel | Comando | Cuándo Ejecutar | Alcance Técnico |
|---|---|---|---|
| **Nivel 1** | `just gauntlet-fast` | Tras cada edición de archivo | Gitleaks (staging), `tsc --noEmit`, ESLint (incluye Gate 10, determinismo del dominio), Vitest unitario, la suite de dominio (`just test-domain`, <1s, sin Postgres) y Gate 9 (justificación de supresiones). |
| **Nivel 2** | `just gauntlet` | Antes de commitear cambios | Aislamiento arquitectónico (depcruise), PostgreSQL tmpfs, deriva DDL (Gate 2), reversibilidad Up/Down (Gate 1), matriz de auth (Gate 4) y detector N+1 (Gate 6). |
| **Nivel 3** | `just gauntlet-full` | Antes de solicitar revisión/PR | Property tests con arbitrarios de dominio real, Schemathesis (Gate 3), semillas deterministas (Gate 7), hold-outs SHA-256 (Gate 5) y mutación sobre diff de dominio (Stryker, `mutate:diff`). |
| **Nivel 4** | `just audit` | Tareas programadas / auditoría | Mutación exhaustiva (no solo el diff) de `src/domain/**` completo. |

---

## 2. Los 10 Gates de Seguridad

1. **Gate 1 (Migraciones Reversibles):** Todo `.sql` requiere su `.down.sql`. El rollback no debe dejar residuos DDL en `pg_dump`.
2. **Gate 2 (Schema Drift):** Modificaciones en `src/infra/db/schema.ts` requieren correr `just db-generate`. No puede haber discrepancia entre TypeScript y SQL.
3. **Gate 3 (Contratos OpenAPI):** Todo endpoint debe declarar esquemas TypeBox completos, incluyendo códigos de error esperados (ej. `401 Unauthorized`). Schemathesis no tolera respuestas no documentadas.
4. **Gate 4 (Matriz de Autenticación):** Toda ruta en Fastify debe incluir `preHandler: [requireAuth]` o declarar explícitamente `config: { isPublic: true }`.
5. **Gate 5 (Hold-Out Tests):** La suite `tests/holdout/` y su firma `.holdout.sha256` son de **solo lectura**. Si un agente altera aserciones para forzar un pase, el gate aborta. El sello solo se actualiza manualmente mediante `just seal-holdouts`.
6. **Gate 6 (Detector N+1):** Las consultas relacionadas deben resolverse vía joins o batching (`inArray`). Iterar consultas dentro de bucles sobrepasa el límite de `assertMaxQueries` y rompe el gate.
7. **Gate 7 (Semillas Deterministas):** Prohibido usar `Date.now()`, `new Date()` sin fecha fija, o UUIDs dinámicos en `seed.ts`. Las firmas SHA-256 de volcados consecutivos deben ser idénticas.
8. **Gate 8 (Prevención de Secretos):** Gitleaks escanea el área de preparación en cada ciclo rápido.
9. **Gate 9 (Justificación de Supresiones):** Toda supresión en `src/domain/` requiere razón en la misma línea -- de Stryker (`// Stryker disable ...: texto`) o de la regla ESLint del Gate 10 (`// eslint-disable-next-line no-restricted-syntax -- texto`, la sintaxis de descripción que ESLint mismo reconoce; `:` en vez de `--` no suprime nada -- ESLint intenta resolver "no-restricted-syntax: texto" como nombre de regla, falla, y el error original queda sin suprimir además de sumar uno nuevo). Un mutante equivalente real, o una lectura de reloj/aleatoriedad genuinamente inevitable en un borde muy específico, son motivo legítimo de supresión -- pero también son la salida de emergencia más obvia para ponerse en verde sin resolver nada, así que no se aceptan sin justificar. `just gauntlet-fast` (`check-suppressions`) lo exige con un solo grep para ambos casos; el Reviewer revisa cada supresión nueva del diff una por una, no solo que el texto exista.
10. **Gate 10 (Determinismo del Dominio):** `src/domain/**` no lee ambiente no determinista -- ni el reloj (`Date.now()`, `new Date()` sin argumentos, `performance.now()`, `process.hrtime()`), ni aleatoriedad (`Math.random()`), ni la zona horaria del entorno (`Intl.DateTimeFormat()`). Regla ESLint (`no-restricted-syntax`) scopeada a `src/domain/**/*.ts` -- ver `eslint.config.js`. `new Date(valor)` con argumento sigue permitido: construir un instante a partir de un valor recibido es legítimo, leer el reloj no lo es. El tiempo y la aleatoriedad entran como parámetros explícitos desde la capa de aplicación. Una sola lectura intermitente rompe la reproducibilidad de las property tests (fast-check) y hace que los mutantes de Stryker corran distinto entre corridas -- el peor modo de falla posible, porque pasa en verde la mayoría de las veces.

---

## Mutación: qué se prueba y qué no

Stryker (`just gauntlet-full` con `mutate:diff`, `just audit` con `mutate:full`)
muta **solo `src/domain/**`** -- lógica de negocio pura, sin I/O, sin base de datos,
sin reloj, sin dependencias de Fastify. Es una regla, no una sugerencia: si lógica de
negocio se filtra a un handler HTTP o a un adaptador de infraestructura, sale del
alcance de la mutación sin que ningún gate se queje -- ese es el modo de falla que esta
convención existe para prevenir. `dependency-cruiser` (`domain-isolation` en
`.dependency-cruiser.js`) ya bloquea que `src/domain` importe de `infra`/`http`; queda
en el autor y en el Reviewer que la lógica en sí (no solo los imports) viva ahí.

Handlers, adaptadores y cableado (`src/http/`, `src/infra/`) **no** se mutan -- no
porque no importen, sino porque ya tienen cobertura de otro tipo, más barata y de
señal más directa para esas capas: Schemathesis audita el contrato OpenAPI (Gate 3),
la matriz de auth confirma que cada ruta declara `preHandler`/`isPublic` (Gate 4), y el
detector de N+1 (Gate 6) audita el patrón de queries. Mutar esas capas también pagaría
un round trip a Postgres por mutante -- costo alto, señal baja, para un tipo de bug que
esos otros gates ya atrapan de forma más directa.

La suite que Stryker corre por mutante es `vitest.config.domain.ts` (solo
`src/domain/**/*.test.ts`, sin `globalSetup` ni conexión a base de datos) -- ver
`just test-domain`. `coverageAnalysis: 'perTest'` hace que cada mutante corra
únicamente los tests que tocan la línea mutada, no la suite entera.

`incremental: true` en `stryker.config.mjs` (estado en `.stryker-tmp/`, gitignored)
**solo ayuda en local**: en CI cada corrida parte de un checkout limpio, así que el
archivo de estado nunca sobrevive entre corridas y `just gauntlet-full`/`just audit`
en CI siempre corren completo. No hay que cachear `.stryker-tmp/` en CI para esto --
este template no corre `just audit` como tarea programada; si un proyecto generado
agrega ese cron, cachear `.stryker-tmp/` con clave por rama sí vuelve a tener sentido.

`mutate:diff` y `mutate:full`/`just audit` usan archivos de estado incremental
**separados** (`.stryker-tmp/incremental.json` para el primero,
`.stryker-tmp/incremental-audit.json` para el segundo, vía `scripts/run-mutation.sh
--full`). Si compartieran uno solo, `just audit` corrido poco después de un
`mutate:diff` en la misma sesión reutilizaría sus resultados en vez de recorrer
`src/domain/**` completo -- exactamente lo contrario de lo que un tier "auditoría
exhaustiva" promete.

Stryker no reporta en su tabla de resumen cuántos mutantes instrumentados fueron
**ignorados** (`// Stryker disable`, ver Gate 9) -- un "100.00%" puede estar
calculado solo sobre los mutantes que sí corrieron, escondiendo que una fracción
real de los instrumentados nunca se ejecutó. `scripts/run-mutation.sh` (usado por
ambos comandos) lee el reporte JSON (`reports/mutation/mutation.json`, reporter
`'json'` en `stryker.config.mjs`) e imprime ese conteo -- archivo, línea y motivo de
cada mutante ignorado -- después de cada corrida, pase o falle. Un score perfecto
que esconde exclusiones sin decirlo es la métrica que un agente aprende a explotar;
esto la hace imposible de esconder por accidente.

### Dos bugs reales que este trabajo encontró (no los reintroduzcas)

Verificados a mano ejecutando Stryker de verdad, no solo leyendo el código -- ambos
hacían que la mutación pareciera correr pero no corriera nada, en cualquier proyecto
generado desde este template, sin importar el alcance del `mutate`:

1. **`.npmrc` con `node-linker=hoisted` es obligatorio con pnpm.** Sin él,
   `@stryker-mutator/core` no encuentra `@stryker-mutator/vitest-runner` -- el
   layout "isolated" por defecto de pnpm le da a cada paquete su propia vista
   privada de `node_modules`, que solo contiene SUS PROPIAS dependencias
   declaradas, no los devDependencies hermanos del proyecto. `pnpm run
   mutate:full`/`mutate:diff` fallaban con "Cannot find TestRunner plugin
   vitest. In fact, no TestRunner plugins were loaded" el 100% de las veces,
   pasara lo que pasara con `mutate`/`coverageAnalysis`/el vitest config.
2. **`scripts/mutate-diff.sh` usaba pathspecs `'src/domain/**/*.ts'` que git
   nunca matcheaba.** Sin magia `:(glob)` explícita, `**` no es un glob de
   varios niveles para `git diff`/`git ls-files` -- el resultado era siempre
   vacío, así que `mutate:diff` (parte de `just gauntlet-full`, el gate de
   Nivel 3 que corre antes de cada PR) imprimía "Sin cambios detectados" y
   pasaba en verde incluso con cambios de dominio reales sin commitear. El
   fix usa el directorio (`'src/domain'`) como pathspec, sin glob.

Si vas a tocar cualquiera de los dos (el `.npmrc` o los pathspecs de
`mutate-diff.sh`), corre `pnpm run mutate:full` de verdad con un cambio real
en `src/domain/` antes de confiar en que el gate sigue funcionando -- ambos
bugs "se veían bien" leyendo el código.

---

## 3. Las 4 Capas de Enforcement

1. **Capa 1 (Hooks del Agente):**
   - `PreToolUse`: Bloquea cualquier intento de escritura o borrado sobre `tests/holdout/` y `.holdout.sha256`. Falla cerrado si el input no es parseable.
   - `Stop`: Impide que el agente declare terminada la sesión si `just gauntlet-fast` arroja error.
   - `PostToolUse`: Da feedback rápido de ESLint tras cada `Edit`/`Write` sobre un `.ts`, sin bloquear nada (ver tabla de fail-closed/fail-open abajo).
2. **Capa 2 (Git Pre-Commit Hook):** Ejecuta `just gauntlet-fast` antes de registrar cualquier commit en local.
3. **Capa 3 (CI / GitHub Actions):** Ejecuta `just doctor` y `just gauntlet-full` en contenedores limpios para cada push o PR.
4. **Capa 4 (Branch Protection):** La rama `main` exige que el check `Level 3 Pre-Merge Gate` pase en verde antes de autorizar merge.

### Qué falla cerrado y qué falla abierto

**Los componentes de seguridad fallan CERRADOS; los de conveniencia fallan
ABIERTOS.** No es teórico: el hallazgo fundacional de este principio en
este mismo repositorio fue que `guard-holdouts.sh` y `stop-gate.sh` usaban
`exit 1` en vez de `exit 2` -- en Claude Code, solo `exit 2` bloquea un
`PreToolUse`/`Stop`, así que ambos llevaban desde su creación sin bloquear
nada de verdad, pese a que el código "parecía" correcto. Antes de dar por
bueno un componente nuevo, pregúntate qué hace cuando le falta una
dependencia o el evento no puede resolverse, no solo cuando todo está
instalado -- y **provócalo en vivo** al menos una vez; un componente nunca
verificado en el entorno real no cuenta como confirmado.

| Hook | Clasificación | Por qué |
|---|---|---|
| `PreToolUse` (`guard-holdouts.sh`) | Seguridad -- falla CERRADO | Si no se puede determinar la ruta del archivo por cualquier motivo (JSON malformado, campo ausente, `jq` mismo ausente), bloquea por defecto (`exit 2`). |
| `Stop` (`stop-gate.sh`) | Seguridad -- falla CERRADO | Es la última revisión antes de que termine el turno; si `just`/`CLAUDE_PROJECT_DIR` faltaran y esto fallara abierto, un turno entero podría cerrarse sin que `gauntlet-fast` corriera ni una vez. |
| `PostToolUse` (`lint-on-edit.sh`) | Conveniencia -- falla ABIERTO | Si falta `jq`, `pnpm`, o no se puede resolver la ruta, simplemente no da feedback -- nunca debe impedir que una edición ya aplicada quede en pie. Un hallazgo real de ESLint sí se reporta (`exit 2`, no bloqueante para `PostToolUse` porque la herramienta ya corrió, solo aparece como contexto inmediato). |
| CI `doctor` | Seguridad -- falla CERRADO por diseño de dependencia | Si `just doctor` falla (falta una herramienta en el runner), el resto del workflow no debería continuar como si hubiera pasado. |

---

## 4. Roles Operativos y Aislamiento con Git Worktrees

Para evitar colisiones de dependencias, bloqueo de puertos y sobrescritura de ramas locales:

### Rol: Generator (Desarrollo Activo)

- Trabaja en el directorio raíz del repositorio.
- Opera siempre en una rama de feature (`git checkout -b feature/<nombre>`).
- Debe correr `just gauntlet-fast` continuamente y commitear únicamente con `just gauntlet` en verde.
- **Regla, no sugerencia:** la lógica de negocio vive en `src/domain/**` -- pura, sin
  I/O, sin base de datos, sin reloj, sin `Fastify`. Los handlers en `src/http/` y los
  adaptadores en `src/infra/` solo traducen entrada/salida; no deciden. Si una regla de
  negocio termina escrita dentro de un handler, ya salió del alcance de la mutación
  (ver "Mutación: qué se prueba y qué no") sin que ningún gate lo marque -- ese es el
  modo de falla exacto que esta regla existe para prevenir.
- **"Sin reloj" es mecánico, no una promesa verbal:** Gate 10 (ESLint,
  `no-restricted-syntax`) prohíbe `Date.now()`, `new Date()` sin argumentos,
  `performance.now()`, `process.hrtime`, `Math.random()` e
  `Intl.DateTimeFormat()` dentro de `src/domain/**`, corre en `gauntlet-fast`
  (milisegundos) y aborta la sesión si falla. Entérate de la regla leyendo
  esto, no chocando con el hook de `Stop` a mitad de una tarea: si el
  dominio necesita el instante actual o un valor aleatorio, recíbelo como
  parámetro explícito desde la capa de aplicación -- nunca lo leas del
  entorno dentro de `src/domain/**`. `new Date(valor)` con argumento sigue
  permitido.

### Rol: Reviewer (Auditoría de Código)

- **Nunca** inspecciona código dentro del mismo working tree del generador.
- Despliega un entorno desacoplado mediante:
  ```bash
  just review-start <rama-a-revisar>
  ```
- Al terminar, libera el worktree con `just review-clean`.

#### La checklist contra la que te van a revisar

`generator`: saber esto de antemano mejora el diff que entregas, no es
información reservada para el revisor. El detalle completo, con ejemplos,
vive en [`.claude/agents/reviewer.md`](./.claude/agents/reviewer.md) -- lo
que sigue es el resumen. `reviewer` recorre estas ocho letras EN ORDEN,
reporta cada una explícitamente (incluso sin hallazgo), y cierra con un
veredicto máquina-legible; un hallazgo en A, C, D o G bloquea por defecto:

| # | Punto | Qué mira |
|---|---|---|
| A | Tests debilitados | ¿Se borró, relajó o editó una aserción existente? |
| B | Tests tautológicos | ¿El test afirma una invariante, o solo lo que la implementación devuelve hoy? |
| C | Casos especiales | ¿Una rama/guarda nueva satisface un test puntual en vez de corregir la regla general? |
| D | Marcadores de test desactivado | `.skip`, `.only`, `.todo`, `it.fails`, un `describe` comentado. |
| E | Errores tragados | `catch` vacío o que devuelve un default, promesa sin `await`, error convertido en log sin propagar. |
| F | Restricciones vueltas opcionales | Campo obligatorio con default, parámetro que pasó a opcional, tipo ensanchado, umbral subido. |
| G | Supresiones | Toda supresión nueva de Stryker o ESLint, con juicio sobre si la razón es válida -- no solo que exista. |
| H | Fuga de capa | Lógica de dominio fuera de `src/domain/**` (incluida una lectura de reloj/aleatoriedad que Gate 10 no atrapó). |

El revisor sigue sin recibir la conversación donde se escribió el diff --
solo el diff. Eso no cambia con esta checklist.

### Regla de concurrencia: un árbol de trabajo, un escritor

`generator` puede seguir usando el árbol principal (es el único rol con
permiso de escritura; nadie más compite por ese árbol mientras trabaja
solo). Un rol de verificación (`reviewer`, o cualquier otro que se agregue
después) **nunca** debe inspeccionar un árbol que otro proceso pueda estar
modificando al mismo tiempo -- si dos roles de verificación corren en
paralelo entre sí, o junto con `generator`, cada uno necesita su propio
`git worktree` vía `just review-start <nombre>` / `just review-clean`. Esto
es intencionalmente independiente de cualquier IDE o entorno de agentes en
particular: se apoya solo en `git worktree`, así que funciona igual sin
importar qué herramienta orquesta las sesiones paralelas.

Si un rol de verificación detecta a medio trabajo que el árbol cambió bajo
sus pies (una rama distinta, archivos apareciendo o desapareciendo sin
haberlos tocado), debe abortar y reportarlo -- nunca seguir adelante con
resultados medidos sobre un estado inconsistente como si fueran confiables.
