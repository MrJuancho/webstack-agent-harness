import { describe, it, expect } from 'vitest';

describe('Canary Test Suite', () => {
  it('garantiza que el arnés de tests evalúa aserciones correctamente', () => {
    expect(true).toBe(true);
  });
});
